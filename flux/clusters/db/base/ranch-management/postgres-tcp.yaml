apiVersion: v1
kind: Service
metadata:
  name: ranch-management-tailscale
  namespace: ranch-management
  annotations:
    tailscale.com/hostname: ranch-management-tcp
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app.kubernetes.io/name: ranch-management-tcp
  ports:
  - name: ranch-management-tcp
    port: 443
    targetPort: 5432
    protocol: TCP