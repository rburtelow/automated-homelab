apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-system
spec:
  instances: 1

  storage:
    size: 10Gi

  bootstrap:
    initdb:
      database: ranch-management
      owner: ranch-management
      secret:
        name: ranch-management-db-password