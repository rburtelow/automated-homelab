apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: diaper-party
  namespace: diaper-party
spec:
  instances: 1

  storage:
    size: 2Gi

  bootstrap:
    initdb:
      database: diaper-party
      owner: diaper-party
      secret:
        name: diaper-party-db-password