apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: awx-operator
  namespace: awx-operator
spec:
  interval: 10m0s
  timeout: 5m0s
  chart:
    spec:
      chart: awx-operator/awx-operator
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: awx-operator
        namespace: awx-operator
      interval: 10m0s 
 