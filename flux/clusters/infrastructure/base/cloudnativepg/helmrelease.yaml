apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnativepg
  namespace: cnpg-system
spec:
  interval: 10m0s
  timeout: 10m0s
  chart:
    spec:
      chart: cloudnative-pg
      version: ">=0.0.0"
      sourceRef:
        kind: HelmRepository
        name: cloudnativepg
        namespace: cnpg-system
      interval: 10m0s
  values:
    crds:
      create: true
