apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: arc-runner-set
  namespace: arc-runners
spec:
  releaseName: arc-runner-set
  chart:
    spec:
      chart: gha-runner-scale-set
      sourceRef:
        kind: HelmRepository
        name: gha-runner-scale-set
        namespace: arc-runners
  interval: 30m
  install:
    remediation:
      retries: 3
  values:
    githubConfigUrl: "https://github.com/rburtelow/getajob"
    githubConfigSecret: arc-runner-secret
    containerMode:
      type: dind
    controllerServiceAccount:
      namespace: arc-systems
      name: arc-gha-rs-controller
    template:
      spec:
        containers:
          - name: runner
            securityContext:
              capabilities:
                add: ["NET_ADMIN"]
